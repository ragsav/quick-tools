---
import TimeToolLayout from "../../layouts/TimeToolLayout.astro";
---

<TimeToolLayout
  title="Working Days Calculator - Count Business Days Between Dates"
  description="Calculate the number of working days (excluding weekends) between two dates. Free online business day counter."
  canonicalPath="/time/working-days-calculator"
  currentTool="Working Days Calculator"
>
  <div class="tool-header">
    <h1 class="text-2xl sm:text-3xl font-bold text-slate-900 mb-2">Working Days Calculator</h1>
    <p class="text-slate-500">
      Count the number of working days (Monday–Friday) between two dates, excluding weekends.
    </p>
  </div>

  <div class="bg-slate-50 border border-slate-200 rounded p-6 mb-8">
    <div class="flex flex-wrap gap-6 mb-6">
      <div class="flex-1 min-w-48">
        <label for="startDate" class="block font-medium text-slate-700 mb-1.5 text-sm">Start Date</label>
        <input type="date" id="startDate" class="w-full px-4 py-3 text-base border border-slate-300 rounded bg-white text-slate-800 outline-none" />
      </div>
      <div class="flex-1 min-w-48">
        <label for="endDate" class="block font-medium text-slate-700 mb-1.5 text-sm">End Date</label>
        <input type="date" id="endDate" class="w-full px-4 py-3 text-base border border-slate-300 rounded bg-white text-slate-800 outline-none" />
      </div>
    </div>

    <div class="mb-4">
      <label class="flex items-center gap-2 text-sm text-slate-600">
        <input type="checkbox" id="includeSaturday" class="w-4 h-4" />
        Include Saturdays as working days
      </label>
    </div>

    <button class="btn-primary inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-white rounded cursor-pointer" onclick="calculate()">
      Calculate Working Days
    </button>

    <div class="result-box mt-6 p-4 bg-white rounded hidden" id="resultBox">
      <div class="text-xs text-slate-500 uppercase tracking-wider mb-1">Working Days</div>
      <div class="text-3xl font-bold text-slate-900" id="result">—</div>
      <div class="text-sm text-slate-500 mt-2" id="breakdown"></div>
    </div>
  </div>

  <section class="mt-8 pt-6 border-t border-slate-200">
    <h2 class="text-xl font-semibold text-slate-900 mb-4">How It Works</h2>
    <p class="text-slate-600 mb-3">This calculator counts only weekdays (Monday through Friday) between your selected dates. Weekends (Saturday and Sunday) are automatically excluded unless you check the "Include Saturdays" option.</p>
    <ul class="list-disc pl-6 text-slate-600 space-y-2">
      <li>Select your start and end dates</li>
      <li>Optionally include Saturdays</li>
      <li>Get the count of working days instantly</li>
    </ul>
  </section>

  <section class="mt-8 pt-6 border-t border-slate-200">
    <h2 class="text-xl font-semibold text-slate-900 mb-4">FAQ</h2>
    <h3 class="text-base font-semibold text-slate-700 mt-4 mb-2">Does this include holidays?</h3>
    <p class="text-slate-600">No, this calculator only excludes weekends. For holiday exclusion, use our Business Days Calculator.</p>
    <h3 class="text-base font-semibold text-slate-700 mt-4 mb-2">Are both dates included?</h3>
    <p class="text-slate-600">The start date is included, but the end date is excluded (like counting days until a deadline).</p>
  </section>

  <script is:inline>
    function calculate() {
      const startInput = document.getElementById("startDate");
      const endInput = document.getElementById("endDate");
      const includeSat = document.getElementById("includeSaturday").checked;
      const resultEl = document.getElementById("result");
      const breakdownEl = document.getElementById("breakdown");
      const resultBox = document.getElementById("resultBox");

      const start = new Date(startInput.value);
      const end = new Date(endInput.value);

      if (isNaN(start.getTime()) || isNaN(end.getTime())) {
        resultEl.innerText = "Please select both dates";
        breakdownEl.innerText = "";
        resultBox.classList.remove("hidden");
        return;
      }

      let count = 0;
      let totalDays = 0;
      let current = new Date(start);

      while (current < end) {
        totalDays++;
        const day = current.getDay();
        if (day !== 0 && (day !== 6 || includeSat)) {
          count++;
        }
        current.setDate(current.getDate() + 1);
      }

      resultEl.innerText = count + " working days";
      breakdownEl.innerText = `Out of ${totalDays} total days (${totalDays - count} weekend days excluded)`;
      resultBox.classList.remove("hidden");
    }
  </script>
</TimeToolLayout>
